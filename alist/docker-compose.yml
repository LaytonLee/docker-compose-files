version: '3.3'
services:
  alist:
    image: 'xhofe/alist:latest'
    container_name: alist
    restart: always
    volumes:
      - '/etc/alist:/opt/alist/data'
    ports:
      - '5244:5244'
    environment:
      - PUID=1000
      - PGID=1000
      - UMASK=022
